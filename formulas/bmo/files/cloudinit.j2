#cloud-config
users:
  - default
  - name: gcr
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    lock_passwd: false
    ssh_pwauth: True
    passwd: "{{ pass }}"
    ssh_authorized_keys:
        - {{ pillar['node_deploy_key'] }}