{
  "links": [
    {
      "id": "{{ pillar['hosts']['compute']['interface'] }}",
      "type": "phy",
      "ethernet_mac_address": "{{ mac }}"
    }
  ],
  "networks": [
    {
      "id": "{{ pillar['hosts']['compute']['interface'] }}",
      "link": "{{ pillar['hosts']['compute']['interface'] }}",
      "type": "ipv4",
      "host": "{{ ip }}",
      "netmask": "{{ pillar['networking']['subnets']['management'] }}",
      "gateway": "{{ gateway }}"
    }
  ],
  "services": [
    {
      "type": "dns",
      "address": "{{ nameserver }}"
    }
  ]
}