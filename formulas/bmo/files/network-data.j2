network:
  version: 2
  ethernets:
    enp1s0:
      match:
        macaddress: {{ mac }}
      dhcp4: false
      addresses:
        - {{ ip }}/{{ prefix }}
      gateway4: {{ gateway }}
      nameservers:
        search: [ {{ domain }} ]
        addresses: [ {{nameserver}} ]