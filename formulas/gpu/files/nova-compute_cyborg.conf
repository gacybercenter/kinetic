{% set mdev_types = mdev_type.keys() | list %}
[DEFAULT]
compute_driver=libvirt.LibvirtDriver
[libvirt]
virt_type=kvm
[devices]
enabled_mdev_types = {{ mdev_types }}
{% for type in mdev_type %}
[mdev_{{ type }}]
device_addresses = {{ busid }}
{% endfor %}