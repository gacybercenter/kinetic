{% set mdev_types = mdev_type.keys() | list %}
[DEFAULT]
compute_driver=libvirt.LibvirtDriver
[libvirt]
virt_type=kvm
[devices]
enabled_mdev_types = {% ','.join(mdev_types) %}
{% for type,bus in mdev_type %}
[mdev_{{ type }}]
device_addresses = {{ bus }}
{% endfor %}