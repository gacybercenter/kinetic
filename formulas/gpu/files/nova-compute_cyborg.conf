{% set mdev_types = list({{ mdev_type }}.keys()) %}
[DEFAULT]
compute_driver=libvirt.LibvirtDriver
[libvirt]
virt_type=kvm
[devices]
enabled_mdev_types = {{ mdev_types }}
{% for type in in mdev_type %}
[mdev_{{ type }}]
device_addresses = {{ busid }}
{% endfor %}