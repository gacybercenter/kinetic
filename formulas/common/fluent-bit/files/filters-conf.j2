[FILTER]
    Name         modify
    Match        *
    Add          host_id ${HOSTNAME}
    Rename       message log

[FILTER]
    Name         kubernetes
    Match        libvirt
    Merge_Log    On
    Keep_Log     Off
    K8S-Logging.Parser On