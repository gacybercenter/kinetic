<source>
  @type syslog
  port 5514
  bind 0.0.0.0
  tag infra.{{ service }}.syslog
  severity_key log_level
  source_hostname_key Hostname
  <parse>
    @type regexp
    expression /^(?<Payload>.*)$/
  </parse>
</source>