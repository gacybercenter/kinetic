include:
  - /formulas/common/helm/install
  - /formulas/common/k8s

# Multus is a CNI meta-plugin that enables attaching multiple network interfaces to pods.
# The helm chart (multus-cni) will be applied in init.sls.
